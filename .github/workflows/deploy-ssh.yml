name: Deploy via cPanel Git

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: cPanel Git deployment notice
        run: |
          echo "Automatic SSH deployment is disabled for this project."
          echo "This hosting account does not have shell access enabled."
          echo "Use cPanel Git Version Control to pull the latest repository changes into /home/mubcaa/site.mubcaa.com."
